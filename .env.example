# Fallback base path for the medical archive (all 3 layers stored here).
# Used only when the current working directory is NOT itself an archive root.
# If you keep multiple archives (one per person), `cd` into the archive's root
# folder to work with it, or pass --base-path explicitly per command.
# Example: /Users/you/medical-archive
BASE_PATH=
